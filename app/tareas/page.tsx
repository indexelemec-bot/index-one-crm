"use client";
import { Check, CirclePlus, Clock3 } from "lucide-react";
import { useState } from "react";
import { useCrm } from "@/components/crm-provider";
import { Modal, PageHeader } from "@/components/ui";

export default function TareasPage(){const {tasks,opportunities,accounts,currentUser,addTask,completeTask}=useCrm();const [open,setOpen]=useState(false);const [completeId,setCompleteId]=useState<string|null>(null);const mine=tasks.filter((t)=>t.ownerId===currentUser.id||["superadmin","gerencia_comercial"].includes(currentUser.role));const accountFor=(opportunityId:string)=>{const o=opportunities.find((x)=>x.id===opportunityId);return accounts.find((a)=>a.id===o?.accountId)};function add(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);addTask({id:`t${Date.now()}`,opportunityId:String(f.get("opportunityId")),title:String(f.get("title")),dueAt:new Date(String(f.get("dueAt"))).toISOString(),priority:String(f.get("priority")) as "alta"|"media"|"baja",status:"pendiente",ownerId:currentUser.id});setOpen(false)}function finish(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const outcome=String(f.get("outcome"));const next=String(f.get("nextAction"));const due=String(f.get("nextDue"));const old=tasks.find((t)=>t.id===completeId);completeTask(completeId!,outcome,next&&due&&old?{id:`t${Date.now()}`,opportunityId:old.opportunityId,title:next,dueAt:new Date(due).toISOString(),priority:"media",status:"pendiente",ownerId:old.ownerId}:undefined);setCompleteId(null)}return <><PageHeader eyebrow="Disciplina comercial" title="Tareas y próximas acciones" description="Toda oportunidad activa conserva un responsable, un resultado y el siguiente movimiento comercial."><button className="button button-primary" onClick={()=>setOpen(true)}><CirclePlus size={18}/> Nueva tarea</button></PageHeader><div className="card"><div className="section-head"><div><h2>Agenda comercial</h2><p>{mine.filter((t)=>t.status!=="completada").length} acciones abiertas</p></div><Clock3 size={19} color="#f47721"/></div>{mine.sort((a,b)=>a.dueAt.localeCompare(b.dueAt)).map((task)=><div className={`task-row ${task.status==="completada"?"done":""}`} key={task.id}><button className="task-check" onClick={()=>task.status!=="completada"&&setCompleteId(task.id)} aria-label="Completar tarea">{task.status==="completada"&&<Check size={15} color="white"/>}</button><div className="task-copy"><b><span className={`priority priority-${task.priority}`}/>{task.title}</b><small>{accountFor(task.opportunityId)?.name} · {new Date(task.dueAt).toLocaleString("es-DO",{dateStyle:"medium",timeStyle:"short"})}</small>{task.outcome&&<small>Resultado: {task.outcome}</small>}</div><span className={`status-pill status-${task.status==="vencida"?"inactive":"active"}`}>{task.status}</span></div>)}</div>{open&&<Modal title="Crear tarea comercial" onClose={()=>setOpen(false)}><form onSubmit={add}><div className="form-grid"><label className="field field-wide"><span>Oportunidad</span><select name="opportunityId">{opportunities.filter((o)=>!["cliente_activo","perdida"].includes(o.stage)).map((o)=><option value={o.id} key={o.id}>{accountFor(o.id)?.name}</option>)}</select></label><label className="field field-wide"><span>Acción</span><input name="title" required placeholder="Ej. Llamar al presidente de la junta"/></label><label className="field"><span>Fecha y hora</span><input name="dueAt" type="datetime-local" required/></label><label className="field"><span>Prioridad</span><select name="priority"><option value="alta">Alta</option><option value="media">Media</option><option value="baja">Baja</option></select></label></div><div className="form-actions"><button type="button" className="button" onClick={()=>setOpen(false)}>Cancelar</button><button className="button button-primary">Guardar tarea</button></div></form></Modal>}{completeId&&<Modal title="Completar seguimiento" description="Registra el resultado y deja programada la próxima acción." onClose={()=>setCompleteId(null)}><form onSubmit={finish}><div className="form-grid"><label className="field field-wide"><span>Resultado obtenido</span><textarea name="outcome" required/></label><label className="field"><span>Próxima acción</span><input name="nextAction" placeholder="Obligatoria si la oportunidad sigue activa" required/></label><label className="field"><span>Fecha de la próxima acción</span><input name="nextDue" type="datetime-local" required/></label></div><div className="form-actions"><button type="button" className="button" onClick={()=>setCompleteId(null)}>Cancelar</button><button className="button button-primary">Completar y programar</button></div></form></Modal>}</>}
